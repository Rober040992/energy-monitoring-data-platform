{
  "name": "energy-monitoring-data-platform",
  "private": true,
  "scripts": {
    "dev": "cross-env PYTHONPATH=src python -m uvicorn app.main:app --reload --log-level debug",
    "test": "python -m pytest",
    "data_process": "python -m data_processing.run_data_process",
    "lint": "python -m ruff check .",
    "format": "python -m ruff format ."
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
